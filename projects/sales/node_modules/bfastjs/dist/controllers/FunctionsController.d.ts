import { HttpClientAdapter, RestRequestConfig } from "../adapters/HttpClientAdapter";
import { AuthController } from "./AuthController";
export declare class FunctionsController {
    private readonly functionPath;
    private readonly restApi;
    private readonly authAdapter;
    private readonly appName;
    constructor(functionPath: string, restApi: HttpClientAdapter, authAdapter: AuthController, appName?: string);
    post<T>(body?: {
        [key: string]: any;
    }, config?: RestRequestConfig): Promise<T>;
    delete<T>(config?: RestRequestConfig): Promise<T>;
    get<T>(config?: RestRequestConfig): Promise<T>;
    put<T>(body?: {
        [p: string]: any;
    }, config?: RestRequestConfig): Promise<T>;
}
