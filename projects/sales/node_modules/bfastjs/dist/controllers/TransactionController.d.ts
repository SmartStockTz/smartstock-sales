import { TransactionModel } from "../models/TransactionModel";
import { HttpClientAdapter } from "../adapters/HttpClientAdapter";
import { RulesController } from "./RulesController";
import { QueryModel } from "../models/QueryModel";
import { UpdateModel } from "../models/UpdateOperation";
export declare class TransactionController {
    private readonly appName;
    private readonly restApi;
    private readonly rulesController;
    private transactionRequests;
    constructor(appName: string, restApi: HttpClientAdapter, rulesController: RulesController);
    commit(options?: {
        before: (transactionRequests: TransactionModel[]) => Promise<TransactionModel[]>;
        after?: () => Promise<void>;
        useMasterKey?: boolean;
    }): Promise<any>;
    create(domain: string, data: any | any[]): TransactionController;
    delete(domain: string, payload: {
        query: QueryModel;
    }): TransactionController;
    update(domain: string, payload: {
        query: QueryModel;
        update: UpdateModel;
    } | {
        query: QueryModel;
        update: UpdateModel;
    }[]): TransactionController;
}
