import { MatSidenav } from '@angular/material/sidenav';
export declare abstract class DeviceInfoUtil {
    getWidth(): number;
    enoughWidth(): boolean;
    openDrawer(sidenav: MatSidenav): void;
}
