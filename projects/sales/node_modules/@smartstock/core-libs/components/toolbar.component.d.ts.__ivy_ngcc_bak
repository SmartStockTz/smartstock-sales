import { EventEmitter, OnInit } from '@angular/core';
import { MatSidenav } from '@angular/material/sidenav';
import { Router } from '@angular/router';
import { FormControl } from '@angular/forms';
import { StorageService } from '../services/storage.service';
import { UserService } from '../services/user.service';
import { UserModel } from '../models/user.model';
import { EventService } from '../services/event.service';
export declare class ToolbarComponent implements OnInit {
    private readonly router;
    private readonly storage;
    private readonly userDatabase;
    private readonly eventService;
    heading: string;
    showProgress: boolean;
    sidenav: MatSidenav;
    hasBackRoute: boolean;
    backLink: string;
    cartdrawer: MatSidenav;
    showSearch: boolean;
    searchCallback: EventEmitter<string>;
    searchInputControl: FormControl;
    searchPlaceholder: string | 'Type to search';
    currentUser: UserModel;
    noOfProductsInCart: any;
    searchProgressFlag: boolean;
    isMobile: boolean;
    constructor(router: Router, storage: StorageService, userDatabase: UserService, eventService: EventService);
    ngOnInit(): void;
    getProductsInCart(): void;
    logout(): void;
    private _clearSearchInputListener;
}
