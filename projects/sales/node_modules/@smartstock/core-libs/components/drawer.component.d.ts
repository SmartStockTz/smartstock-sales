import { OnInit } from '@angular/core';
import { Observable } from 'rxjs';
import { EventService } from '../services/event.service';
import { UserService } from '../services/user.service';
import { LogService } from '../services/log.service';
import { ShopModel } from '../models/shop.model';
import * as ɵngcc0 from '@angular/core';
export declare class DrawerComponent implements OnInit {
    private readonly userService;
    private readonly logger;
    private readonly eventApi;
    constructor(userService: UserService, logger: LogService, eventApi: EventService);
    shop: ShopModel;
    currentUser: any;
    versionNumber: Observable<string>;
    selectedMenu: {
        background: string;
        borderTopRightRadius: string;
        borderBottomRightRadius: string;
        color: string;
    };
    ngOnInit(): void;
    shouldExpand(route: string): boolean;
    isManager(): boolean;
    isAdmin(): boolean;
    static ɵfac: ɵngcc0.ɵɵFactoryDef<DrawerComponent, never>;
    static ɵcmp: ɵngcc0.ɵɵComponentDefWithMeta<DrawerComponent, "smartstock-drawer", never, { "versionNumber": "versionNumber"; }, {}, never, never>;
}

//# sourceMappingURL=drawer.component.d.ts.map